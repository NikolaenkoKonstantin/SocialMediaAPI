CREATE TABLE friendship
(
    id            INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    first_friend  int references "user" (id) not null,
    second_friend int references "user" (id) not null
)