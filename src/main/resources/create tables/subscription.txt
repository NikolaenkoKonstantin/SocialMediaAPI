CREATE TABLE subscription
(
    id         INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    subscriber int references "user" (id) not null,
    publisher  int references "user" (id) not null,
    type       varchar(15)                not null
)