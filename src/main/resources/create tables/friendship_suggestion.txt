CREATE TABLE friendship_suggestion
(
    id       INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    sender   int references "user" (id) not null,
    consumer int references "user" (id) not null
)